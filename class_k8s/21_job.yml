apiVersion: batch/v1
kind: Job
metadata:
   name: my-job
spec:
   template:
     metadata:
       labels:
         app: myapp
     spec:
        restartPolicy: OnFailure
        containers:
          - image: luksa/batch-job
            name: my-job-cont
