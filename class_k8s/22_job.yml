apiVersion: batch/v1
kind: Job
metadata:
   name: myjob
spec:
   completions: 5
   parallelism: 2
   template:
     metadata:
       labels:
         app: myapp
     spec:
       # restartPolicy: OnFailure
        restartPolicy: Never
        containers:
          - image: luksa/batch-job
            name: mycont
